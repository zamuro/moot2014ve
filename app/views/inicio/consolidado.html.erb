<% if current_usuario.admin? %>
<h1>Posición consolidada</h1>

<table class="table table-striped table-hover ">
	<thead>
		<tr>
			<th>Usuarios registrados</td>
			<th>Usuarios Inscritos</td>
			<th>Pagos Registrados</td>
			<th>Usuarios con pagos realizados</td>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><%= @usuarios.count %></td>
			<td><%= @datos_usuarios.count %></td>
			<td><%= @pago.count %></td>
			<td><%= @pago.distinct.count %></td>
		</tr>
	</tbody>
</table>
<% end %>






