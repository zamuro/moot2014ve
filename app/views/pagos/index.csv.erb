<%- headers = ['Correo electrónico', 'Número de comprobante', 'Banco', 'Monto', 'Fecha del depósito', 'Fecha de carga al sistema'] -%>
<%= CSV.generate_line headers %>
<%- @pagos.each do |x| -%>
  <%= CSV.generate_line([x.usuario.email, x.voucher, x.banco.banco, x.monto, x.fecha, x.created_at]) %>
<%- end -%>